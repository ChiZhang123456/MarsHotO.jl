# Atmosphere data

The packaged MGITM files are currently stored in the repository-level
`MGITM` directory to avoid duplicating approximately 400 MB of tracked data.
`load_mgitm_subsolar_profile` reads those files directly.

Future processed or reduced atmosphere products can be stored here.
